{
  "name": "subgraph",
  "version": "1.0.0",
  "description": "The Graph subgraph for BlockDoc local development",
  "main": "index.js",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "create-local": "graph create --node http://localhost:8020/ blockdoc",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 blockdoc",
    "remove-local": "graph remove --node http://localhost:8020/ blockdoc"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@graphprotocol/graph-ts": "^0.35.1"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.81.0"
  }
}
